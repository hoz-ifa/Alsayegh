name: Alsayegh Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build with Buildozer
        uses: ArtemShatriy/buildozer-action@v1
        with:
          buildozer_version: stable
