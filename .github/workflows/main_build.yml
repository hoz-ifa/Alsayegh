name: Build Alsayegh APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Buildozer
        uses: ArtemShatriy/buildozer-action@v1.6.1
        with:
          workdir: .
          buildozer_version: stable
